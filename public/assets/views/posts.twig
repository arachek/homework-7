<!DOCTYPE html>
<html>
<head>
    <title>Posts</title>
</head>
<body>
<h1>Posts</h1>
<ul>
    {% for post in posts %}
        <li>{{ post.id }}</li>
        <p>{{ post.title }}</p>
        <p>{{ post.content }}</p>
    {% endfor %}
</ul>
</body>
</html>
